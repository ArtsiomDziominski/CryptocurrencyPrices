<Window x:Class="CryptoPrice.Views.AlertsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Price Alerts"
        Width="460" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1E1E2E"
        WindowStyle="ToolWindow">

    <Window.Resources>
        <!-- Dark toggle button for ComboBox -->
        <ControlTemplate x:Key="DarkComboBoxToggle" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2"
                        CornerRadius="4"
                        Background="#FF2A2A3E"
                        BorderBrush="#55FFFFFF"
                        BorderThickness="1"/>
                <Border Grid.Column="0" CornerRadius="4,0,0,4"
                        Background="Transparent" Margin="1"/>
                <Path Grid.Column="1" x:Name="Arrow"
                      Fill="#99FFFFFF"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#FF353548"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Dark ComboBox style -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource DarkComboBoxToggle}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="8,3,24,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#FF2A2A3E"
                                            BorderBrush="#55FFFFFF"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Margin="0,1,0,0"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBoxItem style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF3A3A50"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF4C8BF5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18,14,18,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Add Alert Form ──────────────────────────── -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="New Alert"
                       Foreground="#CCFFFFFF" FontFamily="Segoe UI"
                       FontSize="13" FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Symbol -->
                <ComboBox x:Name="SymbolCombo"
                          Grid.Column="0"
                          Style="{StaticResource DarkComboBox}"/>

                <!-- Target price -->
                <TextBox x:Name="PriceBox"
                         Grid.Column="2"
                         FontFamily="Segoe UI" FontSize="13"
                         Padding="6,4"
                         Background="#FF2A2A3E" Foreground="White"
                         BorderBrush="#55FFFFFF" CaretBrush="White">
                    <TextBox.ToolTip>Target price (e.g. 100000)</TextBox.ToolTip>
                </TextBox>
            </Grid>

            <!-- Options row -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <CheckBox x:Name="ChkSound" Grid.Column="0"
                          Content="Sound" IsChecked="True"
                          Foreground="#CCFFFFFF" FontFamily="Segoe UI" FontSize="12"
                          Margin="0,0,16,0" VerticalAlignment="Center"/>

                <CheckBox x:Name="ChkFlash" Grid.Column="1"
                          Content="Flash widget" IsChecked="True"
                          Foreground="#CCFFFFFF" FontFamily="Segoe UI" FontSize="12"
                          Margin="0,0,16,0" VerticalAlignment="Center"/>

                <Button Grid.Column="3" Content="Add Alert" Width="90"
                        Click="AddAlert_Click"
                        Background="#FF4C8BF5" Foreground="White"
                        BorderThickness="0" Padding="4,5"
                        FontFamily="Segoe UI" FontWeight="SemiBold" Cursor="Hand"/>
            </Grid>

            <Separator Background="#33FFFFFF" Margin="0,0,0,12"/>
        </StackPanel>

        <!-- ── Alert List ──────────────────────────────── -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,12">
            <ItemsControl x:Name="AlertsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FF2A2A3E" CornerRadius="6"
                                Padding="10,8" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Enable toggle -->
                                <CheckBox Grid.Column="0"
                                          IsChecked="{Binding IsEnabled}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,10,0"
                                          Checked="AlertToggle_Changed"
                                          Unchecked="AlertToggle_Changed"
                                          ToolTip="Enable / Disable"/>

                                <!-- Alert info -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Foreground="White" FontFamily="Segoe UI" FontSize="13">
                                        <Run Text="{Binding SymbolLabel, Mode=OneWay}" FontWeight="SemiBold"/>
                                        <Run Text=" @ " Foreground="#99FFFFFF"/>
                                        <Run Text="{Binding PriceLabel, Mode=OneWay}"
                                             Foreground="#FF4C8BF5" FontWeight="SemiBold"/>
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding OptionsLabel, Mode=OneWay}"
                                                   Foreground="#66FFFFFF" FontFamily="Segoe UI"
                                                   FontSize="10"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Delete button -->
                                <Button Grid.Column="3"
                                        Content="&#xE74D;"
                                        FontFamily="Segoe MDL2 Assets" FontSize="14"
                                        Foreground="#99FFFFFF" Background="Transparent"
                                        BorderThickness="0" Cursor="Hand"
                                        VerticalAlignment="Center"
                                        Click="DeleteAlert_Click"
                                        Tag="{Binding Id}"
                                        ToolTip="Delete alert">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="Bd" Background="Transparent"
                                                                CornerRadius="4" Padding="4,2">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Bd" Property="Background"
                                                                        Value="#33FF4444"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- ── Empty state ─────────────────────────────── -->
        <TextBlock x:Name="EmptyLabel" Grid.Row="1"
                   Text="No alerts configured yet."
                   Foreground="#55FFFFFF" FontFamily="Segoe UI"
                   FontSize="13" FontStyle="Italic"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Visible"/>

        <!-- ── Close ───────────────────────────────────── -->
        <Button Grid.Row="2" Content="Close" Width="80"
                HorizontalAlignment="Right"
                Click="Close_Click" IsCancel="True"
                Background="#FF3A3A4E" Foreground="White"
                BorderBrush="#44FFFFFF" Padding="4"
                FontFamily="Segoe UI"/>
    </Grid>
</Window>
